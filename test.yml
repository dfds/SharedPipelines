parameters:
- name: sourceDirectory
  type: string
  default: $(Build.SourcesDirectory)

steps:
- script: echo "building $(Build.SourcesDirectory) with '${{ parameters.serviceImageName }}'"
- bash: echo "building $(Build.SourcesDirectory) with '${{ parameters.serviceImageName }}'"
- template: security-iac-scan.yml@SharedPipelines
  parameters:
    sourceDirectory21: '${{ parameters.serviceImageName }}'