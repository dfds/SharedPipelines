parameters:
- name: sourceDirectory
  type: string
  default: $(Build.SourcesDirectory)

steps:
- script: echo "building $(Build.SourcesDirectory) with '${{ parameters.sourceDirectory }}'"
- bash: echo "building $(Build.SourcesDirectory) with '${{ parameters.sourceDirectory }}'"
- template: security-iac-scan.yml@SharedPipelines
  parameters:
    sourceDirectory: '{{ parameters.sourceDirectory }}'