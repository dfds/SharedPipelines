parameters:
- name: fullImageUri
  type: string
- name: severity
  type: string
  default: ""
- name: ignoreUnfixed
  type: boolean 
  default: true
  
steps:
- script: |
    mkdir $(Agent.TempDirectory)/trivy -m 777
    docker pull aquasec/trivy:latest
    docker run --rm -v "$(Agent.TempDirectory)/trivy:/report" aquasec/trivy:latest image -f sarif -o /report/trivy.sarif "${{ parameters.fullImageUri }}"
    sudo chmod 755 $(Agent.TempDirectory)/trivy/trivy.sarif
  displayName: 'Scan image 1'
  continueOnError: false
  condition: succeededOrFailed()

- script: |
    mkdir $(Agent.TempDirectory)/trivy -m 777

    docker pull aquasec/trivy:lastest
    
    cmd="docker run --rm -v "$(Agent.TempDirectory)/trivy:/report" aquasec/trivy:latest image "
    if [ "${{ parameters.severity }}" != "" ]
    then
        cmd+="--severity "${{ parameters.severity }}" "
    fi
    
    if [ "${{ parameters.ignoreUnfixed}}" ]
    then
        cmd+="--ignore-unfixed "
    fi

    cmd+="-f sarif -o /report/trivy.sarif "${{ parameters.fullImageUri }}" "
    eval cmd
  displayName: 'Scan image 2'
  condition: succeededOrFailed()

- script: | 
    sudo chmod 755 $(Agent.TempDirectory)/trivy/trivy.sarif
  displayName: Update File
  continueOnError: true
  condition: succeededOrFailed()

- task: CopyFiles@2
  inputs:
    SourceFolder: '$(Agent.TempDirectory)/trivy'
    contents: "*.sarif" 
    targetFolder: $(Agent.TempDirectory)/sast-results/trivy/
  continueOnError: true
  condition: succeededOrFailed()

- task: DeleteFiles@1
  displayName: 'Remove unneeded files'
  inputs:
    SourceFolder: '$(Agent.TempDirectory)'
    contents: "$(Agent.TempDirectory)/trivy/*.sarif"
  continueOnError: true
  condition: succeededOrFailed()