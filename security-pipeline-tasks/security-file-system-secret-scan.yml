parameters:
- name: Directory
  type: string

steps:
- script: |
    docker run -it -v "${{ parameters.sourceDirectory }}:/pwd" trufflesecurity/trufflehog:latest filesystem --directory="/pwd"
  displayName: 'Run File System Scan'
  condition: succeededOrFailed()